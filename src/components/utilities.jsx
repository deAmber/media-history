/**
 * Standard new user placeholder text. Used when waiting for data to be able to show a page.
 * @returns {JSX.Element}
 */
export const NewUserText = () => {
  return <p>It looks like you don't have any data at the moment, so there won't be much to see. Try adding a new media entry in by clicking the "Add new" button in the menu above.</p>
}